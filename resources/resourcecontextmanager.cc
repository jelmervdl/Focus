#include "resourcecontextmanager.h"

#include <fstream>

using namespace std;


Context ResourceContextManager::get_context(std::string const &context_name)
{
    //return read_yaml(d_config_file, context_name); //no more yaml
    
}