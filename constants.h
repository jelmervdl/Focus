#ifndef __CONSTANTS_H__
#define __CONSTANTS_H__

float const picture_to_float_min(-0.5);
float const picture_to_float_max(0.5);
unsigned int const channel_default_size(10);
unsigned int const FLOW_STEP_MAX(100);

#endif

